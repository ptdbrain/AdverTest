# ---- Core (must stay light: CI installs exactly this file) ----
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0

# ---- Numerics: attack generation + metrics ----
numpy>=1.26.0

# ---- Optional extras: install per role, never in CI ----
# Real models (adapters)
# torch>=2.4.0
# ultralytics>=8.3.0            # M1 YOLO11s / M3 RT-DETR
# mmdet>=3.3.0                  # M2 Faster R-CNN
# mmdet3d>=1.4.0                # M5 PointPillars / M6 BEVFusion
#
# Attacks
# torchattacks>=3.5.1           # FGSM / PGD / MI-FGSM / C&W references
# imagecorruptions>=1.1.2       # group A: the 19 ImageNet-C corruptions
# kornia>=0.7.3                 # GPU corruptions (plan §5)
# opencv-python-headless>=4.10  # image ops for weather / patch rendering
#
# Metrics, search, tracking
# pycocotools>=2.0.8            # official AP@[.50:.95]
# optuna>=4.0.0                 # Auto Red-Team Search (plan §5)
# wandb>=0.18.0                 # experiment tracking
#
# Storage / queue (plan §4)
# sqlalchemy>=2.0.0
# alembic>=1.14.0
# psycopg2-binary>=2.9.0
# celery>=5.4.0
# redis>=5.2.0

# ---- KITTI + YOLO11 benchmarking (group C, P-195) — install locally, never in CI ----
# Enables the `kitti` dataset, the `yolo11` adapter and scripts/benchmark_kitti_yolo11.py.
# Without them the catalog still imports and every test either runs or skips cleanly.
# pillow>=10.4.0                # decode KITTI PNGs (src/datasets/kitti.py)
# torch>=2.4.0                  # --index-url https://download.pytorch.org/whl/cu124
# ultralytics>=8.3.0            # M1 YOLO11s (src/adapters/yolo11.py)

# ---- Dev tools ----
ruff>=0.8.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.28.0
