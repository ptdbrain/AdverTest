[pytest]
testpaths = tests
norecursedirs = .git .venv data outputs artifacts frontend node_modules .pytest_temp .pytest_cache_local
addopts = --basetemp=.pytest_temp -o cache_dir=.pytest_cache_local
markers =
    gpu: optional integration test requiring a local checkpoint and CUDA
    models: model-dependent test
    integration: integration test suite
